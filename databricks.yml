# This is a Databricks asset bundle definition for dab_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project
  uuid: ed3444b5-8324-4906-bef9-569c74e3a52b

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405606524101174.14.azuredatabricks.net
    variables:
      catalog: citibike_dev
      schema: ${workspace.current_user.short_name}
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://adb-7405606524101174.14.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/vb@mngenvmcap464197.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/vb@mngenvmcap464197.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: viswa_dbks_dev
      schema: prod
    permissions:
      - user_name: vb@mngenvmcap464197.onmicrosoft.com
        level: CAN_MANAGE
